<% if can? :settings, @current_user %>
<div class="row element-style">
  <div class="col-md-11 form-style">
      <fieldset>
        <div class="row">
          <legend class="col-lg-12">Administración de unidades</legend>
        </div>
        <div class="form-group org-panel">
          <div class="row">
            <div class="col-lg-12">
              <div class="col-lg-2">
                <a href="/units/new">
                  <button type="button" class="btn btn-success">Añadir Unidad</button>
                </a>
              </div>
              <div class="col-lg-4 pull-right">
                <%= form_tag(units_path, method: "get", id: "search-form") do%>
                <%= text_field_tag :search, params[:search], class:"form-control search-input col-lg-2", placeholder: "Buscar unidad" %>
                <%= submit_tag "Buscar", class:"btn btn-primary search-btn" %>
                <% end %>
              </div>
            </div>
          </div>
        </div>
        <div class="form-group org-panel">
          <div class="row">
            <label for="select" class="col-lg-12 control-label">Lista de unidades</label>
            <hr>
          </div>
          <div class="row">
            <div class="col-lg-12">
              <table class="table table-striped table-hover ">
                <thead>
                  <tr>
                    <th>Id</th>
                    <th>Nº Unidad</th>
                    <th>Unidad</th>
                    <th>Dirección</th>
                    <th>Subdirección</th>
                    <th>Acciones</th>
                  </tr>
                </thead>
                <tbody>
                  <% if @units.present? %>
                    <% @units.each do |unit| %>
                        <tr>
                          <td>
                            <%= unit.id %>
                          </td>
                          <td>
                            <%= unit.unit_number %>
                          </td>
                          <td>
                            <%= unit.name %>
                          </td>
                          <td>
                            <%= unit.dir_name %>
                          </td>
                          <td>
                            <%= unit.subdir_name %>
                          </td>
                          <td>
                            <%= link_to(content_tag(:i, nil, class: 'fa fa-search', title: 'Detalles'), action: 'show', id: unit.id) %>
                            <%= link_to(content_tag(:i, nil, class: 'fa fa-pencil', style: 'margin-left:5px;', title: 'Editar'), action: 'edit', id: unit.id) %>
                          </td>
                        </tr>
                    <% end %>
                  <% else %>
                    <p>No existe ninguna unidad que contenga el término "<%= params[:search] %>".</p>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </fieldset>
    <div class="col-md-6">
      <a href="/settings">
        <button type="submit" class="btn btn-primary">Volver</button>
      </a>
    </div>
  </div>
</div>
<% end %>
